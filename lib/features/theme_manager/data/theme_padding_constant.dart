import 'package:flutter/widgets.dart';

const kPadding = 16.0;
const globalPaddingAll = EdgeInsets.all(kPadding);
const globalPaddingHorizontal = EdgeInsets.symmetric(horizontal: kPadding);
const globalPaddingVerticle = EdgeInsets.symmetric(vertical: kPadding);
const globalPaddingOnlyLeft = EdgeInsets.only(left: kPadding);
const globalPaddingOnlyTop = EdgeInsets.only(top: kPadding);
const globalPaddingOnlybottom = EdgeInsets.only(bottom: kPadding);
const globalPaddingOnlyRight = EdgeInsets.only(right: kPadding);
